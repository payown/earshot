const kSkipForwardDuration = Duration(seconds: 30);
const kSkipBackDuration = Duration(seconds: 15);

// How far from the end of an episode to begin buffering the next one.
const kGaplessPreloadThreshold = Duration(seconds: 60);
