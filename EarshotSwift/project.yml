name: Earshot
options:
  bundleIdPrefix: media.payown
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
# Beta is a release-optimized config that carries the IS_BETA_BUILD flag so
# beta-only flows (e.g. the Flutter→SwiftUI migration prompt) ship to testers
# but never to the App Store Release build.
configs:
  Debug: debug
  Beta: release
  Release: release
settings:
  base:
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.0"
    SWIFT_STRICT_CONCURRENCY: minimal
targets:
  Earshot:
    type: application
    platform: iOS
    sources:
      - path: Earshot
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: media.payown.earshot.swift
        INFOPLIST_FILE: Earshot/App/Info.plist
        TARGETED_DEVICE_FAMILY: "1,2"
        DEVELOPMENT_TEAM: "72PH974742"
        CODE_SIGN_STYLE: Automatic
        ENABLE_PREVIEWS: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      configs:
        Debug:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG IS_BETA_BUILD
        Beta:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: IS_BETA_BUILD
        Release:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: ""
  EarshotTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: EarshotTests
    dependencies:
      - target: Earshot
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: media.payown.earshot.swift.tests
        DEVELOPMENT_TEAM: "72PH974742"
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: YES
schemes:
  Earshot:
    build:
      targets:
        Earshot: all
        EarshotTests: [test]
    test:
      targets:
        - EarshotTests
      gatherCoverageData: true
    run:
      config: Debug
