Chapter 61, Build 194

Fixes iCloud queue sync for episodes added automatically during a feed refresh. Auto-queued episodes now publish the same durable queue-change signal as manual queue edits, allowing the other device to receive them.

Development iCloud testing only. Do not deploy the production CloudKit schema.
